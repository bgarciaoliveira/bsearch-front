import React from 'react'

import './styles.css'

const Header = () => (
    <header id="main-header">
    <img alt="logo" src={ require('./logo.png') } />
    Search</header>
)

export default Header